{
  "name": "@noyeet/verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static, stateless receipt verifier. Open index.html or serve dist/.",
  "scripts": {
    "build": "bun build ./src/main.ts --outdir ./dist --target browser --format esm --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noyeet/receipts": "workspace:*",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  }
}
