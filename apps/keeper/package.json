{
  "name": "@noyeet/keeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Continuous guarded executor: reads a position, builds a rebalance intent, submits through the gateway.",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  },
  "license": "MIT"
}
