{
  "name": "noyeet",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/gateway",
    "apps/dashboard"
  ],
  "scripts": {
    "test": "bun test packages apps/gateway",
    "typecheck": "bash -c 'for d in packages/* apps/gateway; do [ -f \"$d/package.json\" ] || continue; (cd \"$d\" && bun run typecheck) || exit 1; done'",
    "purity": "bash scripts/check-purity.sh"
  }
}
